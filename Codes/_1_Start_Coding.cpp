/*__________________________PROBLEM__________________________*/
/*
Start Coding

When learning a new language, we first learn to output some message. Here, we'll start with the famous Hello World message. Now, here you are given a function to complete. Don't worry about the ins and outs of functions, just add the command (cout<<"Hello World") to print Hello World.

Input Format :
No input

Output format :
Hello World
Your Task:
Your task is to complete the function below to print Hello World.

Explanation:
Hello World is printed.
*/

/*__________________________PROGRAM__________________________*/

// { Keyword `{` denotes the beginning of a block of code.
// Driver Keyword `Driver` indicates that this is the driver code.
//Code Keyword `Code` specifies the segment of code.
//Starts Keyword `Starts` signifies the beginning.
//Initial Keyword `Initial` specifies the starting point.
//Template Keyword `Template` defines a basic structure or form.
//for Keyword `for` is used to create a loop that consists of three optional expressions, enclosed in parentheses and separated by semicolons.
//C++ Keyword `C++` specifies the language used.
//Include Keyword `Include` is used to include the contents of another file into the current file.
//<iostream> Identifier `<iostream>` is the name of the header file included.
#include<iostream>  // Header file for input/output operations

//using Keyword `using` introduces a namespace.
//namespace Keyword `namespace` defines a scope.
//std:: Namespace `std` represents the standard namespace.
using namespace std;

//void Keyword `void` specifies that the function does not return any value.
//printHello Identifier `printHello` is the name of the function.
//() Parentheses `()` are used to enclose the parameters of a function or method.
// Function Keyword `Function` signifies the beginning of a function declaration.
void printHello();  // Function declaration

// } Keyword `}` denotes the end of a block of code.
// Driver Keyword `Driver` indicates that this is the driver code.
//Code Keyword `Code` specifies the segment of code.
//Ends Keyword `Ends` signifies the conclusion.

// User Keyword `User` indicates that this is user-defined code.
//Function Keyword `Function` specifies the beginning of a function.
//Template Keyword `Template` denotes a general structure.
//for Keyword `for` is used to create a loop.
//C++ Keyword `C++` specifies the language used.
//Identifier `printHello` is the name of the function.
void printHello(){

    //cout Keyword `cout` is used to print output to the console.
    //<< Keyword `<<` is used to insert data into the output stream.
    // "Hello World" Literal `"Hello World"` represents a string to be printed.
    cout << "Hello World";  // Printing "Hello World" to the console

}

// { Keyword `{` denotes the beginning of a block of code.

// Driver Keyword `Driver` indicates that this is the driver code.
//Code Keyword `Code` specifies the segment of code.
//Starts Keyword `Starts` signifies the beginning.
//. Keyword `.` is a symbol.
//int Keyword `int` specifies the data type.
//main Identifier `main` is the name of the main function.
//() Parentheses `()` are used to enclose the parameters of a function or method.
int main()
{
    // Calling function to print Hello World
    //printHello Identifier `printHello` is the name of the function.
    printHello();  // Function call to print "Hello World"

    //return Keyword `return` signifies the end of the function and returns a value.
    //0 Literal `0` represents a numerical value.
    return 0;  // Returning 0 to indicate successful execution
// } Keyword `}` denotes the end of a block of code.
}
// } Keyword `}` denotes the end of a block of code.
// Driver Keyword `Driver` indicates that this is the driver code.
//Code Keyword `Code` specifies the segment of code.
//Ends Keyword `Ends` signifies the conclusion.
